// Typst manual tweak overrides
// Override colors, fonts, and spacing here without editing templates
// This file is imported AFTER common.typ, so values here take precedence

// === THEME COLOR OVERRIDES ===
// #let accent = rgb("#ff6b6b")  // Example: red accent

// === FONT SIZE OVERRIDES ===
// #let size-body = 10.5pt  // Slightly smaller body text
// #let size-header = 13pt  // Slightly smaller headers

// === SPACING OVERRIDES ===
// #let sp-job-between = 14pt  // Tighter job spacing
// #let sp-bullet-between = 6pt  // Tighter bullet spacing